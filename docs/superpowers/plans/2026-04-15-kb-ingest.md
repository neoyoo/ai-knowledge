# kb-ingest Skill Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox syntax for tracking.

**Goal:** Create the kb-ingest skill that enables fully autonomous ingestion of new source projects into the AI knowledge base

**Architecture:** Single skill markdown file with inline templates and rules. Supporting schema files for self-optimization. Skill reads ontology, dispatches parallel subagents for L2 analysis, produces isolated drafts with semantic L1 patches and review reports.

**Tech Stack:** Claude Code Skill (markdown), YAML schema files

---

## 总览

本计划将 spec `/docs/superpowers/specs/2026-04-15-kb-ingest-design.md` 拆分为 9 个任务：

| Task | 内容 | 产出文件 |
|------|------|---------|
| T1 | Skill 骨架 + frontmatter + 触发条件 | `~/.claude/skills/kb-ingest/kb-ingest.md`（骨架） |
| T2 | Phase 1 — 预分析指令 | skill 文件 Phase 1 章节 |
| T3 | Phase 2 — 深度分析并行 subagent | skill 文件 Phase 2 章节 + subagent prompt 模板 |
| T4 | Phase 3 — L1 语义化 patch | skill 文件 Phase 3 章节 + patch 格式模板 |
| T5 | Phase 4 — 覆盖率扫描 | skill 文件 Phase 4 章节 + coverage-report 模板 |
| T6 | Phase 5 — 分级 review 分类 | skill 文件 Phase 5 章节 |
| T7 | Phase 6 — 报告生成 | skill 文件 Phase 6 章节 + REVIEW.md + meta.yaml 模板 |
| T8 | Phase 7 — 自优化反馈 + schema 骨架文件 | skill 文件 Phase 7 + `schema/ingest-strategies.yaml` + `schema/ingest-learnings.md` |
| T9 | 安装 + 验证 | skill 注册到 settings.json，干运行验证 |

**Skill 安装路径**：`~/.claude/skills/kb-ingest/kb-ingest.md`

**关联 schema 文件**（在知识库项目内，非 skill 目录）：
- `{kb-path}/schema/ingest-strategies.yaml`
- `{kb-path}/schema/ingest-learnings.md`

---

## Task 1：Skill 骨架 + frontmatter + 触发条件

> 目标：创建 skill 文件骨架，写完 frontmatter、触发条件说明、输入规范、前置依赖验证检查清单。

### 步骤

- [ ] **1.1** 创建目录 `~/.claude/skills/kb-ingest/`
- [ ] **1.2** 创建 `~/.claude/skills/kb-ingest/kb-ingest.md`，写入以下完整内容：

```markdown
---
name: kb-ingest
description: "Use when ingesting a new source project into the AI knowledge base. Triggers on phrases like 'ingest X into the knowledge base', 'analyze source project X', 'add X to wiki'. Runs 7-phase autonomous workflow: pre-analysis → parallel L2 writing → L1 semantic patches → coverage scan → tiered review → report generation → self-optimization. All output is isolated in drafts/{source}/ and never modifies wiki/ directly."
---

# kb-ingest — AI 知识库 Ingest Skill

> **职责定位**：写路径。对应 ai-knowledge（读路径）。
> 两个 skill 通过 `wiki/` 目录解耦，互不依赖。

## 触发条件

以下请求会触发本 skill：
- "把 X 项目 ingest 到知识库"
- "分析 X 源码，更新 wiki"
- "为 X 写 L2 页面"
- "知识库扩充：X"

## 输入规范

**最小输入**（必填）：
```
源项目路径（本地目录绝对路径 或 git URL）
```

**可选参数**：

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--source-name` | 覆盖自动推断的源名称 | 从目录名或仓库名推断 |
| `--concepts` | 限定分析的概念维度列表（逗号分隔） | 自动推断全部相关维度 |
| `--kb-path` | 知识库根目录绝对路径 | 当前工作目录 |

**示例调用**：
```
kb-ingest /path/to/project-x
kb-ingest https://github.com/org/repo --source-name=project-x --kb-path=/path/to/kb
kb-ingest /path/to/project-x --concepts=memory-system,query-loop
```

## 前置依赖验证（Pre-flight Checklist）

**在 Phase 1 开始前必须全部通过，任一失败则输出错误信息并终止：**

```
[ ] schema/ontology.yaml          存在且可读
[ ] schema/page-templates/L1.md   存在且可读
[ ] schema/page-templates/L2.md   存在且可读
[ ] wiki/                         目录存在
[ ] 源路径                         存在且可读（git URL 则可 clone）
```

错误信息格式：
```
[kb-ingest ERROR] 前置依赖缺失，终止执行：
  - 缺失：schema/ontology.yaml
  - 缺失：wiki/
请确认知识库根目录正确（当前：{kb-path}），并补全缺失文件后重试。
```

## 产出目录结构

```
drafts/{source-name}/
├── REVIEW.md                      # review 报告（入口文件）
├── meta.yaml                      # 机器可读元数据
├── wiki/
│   ├── {concept}.md.patch         # L1 语义化增量 patch
│   └── _impl/
│       └── {concept}--{source}.md # L2 完整新页面
└── analysis/
    ├── module-map.md              # 模块 → 概念维度映射
    └── coverage-report.md         # 覆盖率扫描结果
```

## 错误处理规范

| 错误类型 | 处理方式 |
|---------|---------|
| 前置依赖缺失 | 输出明确错误信息，列出缺失路径，终止执行 |
| 源路径不存在 | 输出错误信息，终止执行 |
| git clone 失败 | 输出错误信息（含 git 错误原因），终止执行 |
| 单个 subagent L2 写入失败 | 记录失败项，继续其他 subagent，在 REVIEW.md 中标注 |
| 覆盖率低于门禁 | 记录 BLOCKED 状态，写入 REVIEW.md，不终止（继续产出报告） |
| drafts/ 已存在同名目录 | 输出警告，覆盖写入，不终止 |

---
```

- [ ] **1.3** git commit：`feat(skill): add kb-ingest skill skeleton with frontmatter and pre-flight checklist`

---

## Task 2：Phase 1 — 预分析指令

> 目标：在 skill 文件中写入 Phase 1 完整指令，包括 ontology 读取、源项目扫描、模块→概念映射逻辑、产出 module-map.md 格式和 meta.yaml 初始结构。

### 步骤

- [ ] **2.1** 在 skill 文件 `## 错误处理规范` 之后追加以下内容：

```markdown
---

## Phase 1：预分析

### 目标
建立分析计划，验证输入合法性，产出「模块 → 概念维度」映射。

### 执行步骤

**步骤 1：运行前置依赖验证（见"前置依赖验证"章节）**

**步骤 2：读取 Agent 启动配置**
按顺序读取（如文件存在）：
1. `{kb-path}/schema/ingest-strategies.yaml` → 作为执行策略指导
2. `{kb-path}/schema/ingest-learnings.md` → 作为参考经验
若文件不存在，跳过，继续执行。

**步骤 3：读取 ontology**
- 读取 `{kb-path}/schema/ontology.yaml`
- 解析获取 12 个概念维度（名称、aliases、定义）
- 记录每个维度的关键词 aliases，用于后续模块匹配

**步骤 4：推断 source-name**
- 若用户提供 `--source-name`，直接使用
- 本地路径：取最后一段目录名（如 `/path/to/hermes` → `hermes`）
- git URL：取仓库名（如 `https://github.com/org/hermes` → `hermes`）

**步骤 5：验证源路径**
- 本地路径：检查目录存在且可读，若不存在则终止
- git URL：`git clone {url} /tmp/kb-ingest-{timestamp}/`，失败则输出 git 错误信息并终止

**步骤 6：防重复检查**
- 检查 `{kb-path}/drafts/{source-name}/` 是否已存在
- 若存在：输出警告 `[kb-ingest WARN] drafts/{source-name}/ 已存在，将覆盖写入`，继续执行

**步骤 7：创建产出目录结构**
```bash
mkdir -p {kb-path}/drafts/{source-name}/wiki/_impl/
mkdir -p {kb-path}/drafts/{source-name}/analysis/
```

**步骤 8：扫描源项目目录结构**
- 列出所有顶层目录/模块（排除 `.git/`、`__pycache__/`、`node_modules/`）
- 识别主语言：检查是否存在 `pyproject.toml`/`setup.py`（Python）、`package.json`（TypeScript/JS）、`go.mod`（Go）等
- 估算代码规模（文件数、大致行数）

**步骤 9：映射模块 → 概念维度**
- 对每个顶层模块，用 ontology aliases 做关键词匹配（不区分大小写）
- 一个模块可映射到多个概念维度
- 一个概念维度可对应多个模块
- 若 `--concepts` 参数存在，只映射指定的概念维度

**关键词映射参考**（以 ontology 实际内容为准，此处仅示例）：
| 概念维度 | 搜索关键词 |
|---------|----------|
| memory-system | memory, dream, archive, consolidate, compact, recall, forget |
| query-loop | loop, run, step, turn, cycle, execute, dispatch |
| tool-system | tool, call, invoke, register, execute, permission |
| multi-agent | orchestrat, worker, spawn, delegate, coordinate, handoff |
| context-management | context, compress, summarize, window, token, truncate |
| mcp-skills | mcp, plugin, extension, server, client, stdio |

### 产出文件

**`drafts/{source}/analysis/module-map.md`** — 写入以下格式：

```markdown
# 模块 → 概念维度映射：{source}

**生成时间**: {YYYY-MM-DD HH:MM}
**源路径**: {source-path}
**主语言**: {language}
**代码规模**: 约 {N} 个文件

## 顶层模块列表

| 模块路径 | 主要功能（推断） | 映射概念维度 |
|---------|---------------|------------|
| src/memory/ | 记忆存储与检索 | memory-system |
| src/agent/ | Agent 主循环 | query-loop |
| src/tools/ | 工具注册与执行 | tool-system |
| src/utils/ | 工具函数 | （通用，不单独写 L2） |
| ... | ... | ... |

## 概念维度覆盖计划

| 概念维度 | 相关模块 | 是否派 subagent |
|---------|---------|---------------|
| memory-system | src/memory/, src/utils/persistence/ | 是 |
| query-loop | src/agent/, src/core/ | 是 |
| tool-system | src/tools/ | 是 |
| prompt-system | （未发现明显相关模块） | 否 |
| ... | ... | ... |

## 排除模块（不计入覆盖率分母）

- `.github/`：CI/CD 配置，非核心实现
- `docs/`：文档，非源码
- `tests/`：测试代码，不单独分析
- `examples/`：示例代码
```

**`drafts/{source}/meta.yaml`** — 写入初始状态：

```yaml
source_name: "{source-name}"
source_path: "{original-path}"
source_version: ""          # Phase 2 中从 git log 获取后填入
ingest_time: "{ISO-8601-datetime}"
kb_path: "{knowledge-base-root}"
concepts_covered: []        # Phase 2 完成后更新
coverage_rate: null         # Phase 4 完成后更新
l2_files: []               # Phase 2 完成后更新
l1_patches: []             # Phase 3 完成后更新
status: in-progress
review_summary:
  auto_fixed: 0
  pending_review: 0
  blocked: 0
```
```

- [ ] **2.2** git commit：`feat(skill): add Phase 1 pre-analysis instructions with module mapping and output templates`

---

## Task 3：Phase 2 — 深度分析（并行 subagent 派发）

> 目标：写入 Phase 2 完整指令，包括 subagent prompt 模板（完整内联）、L2 质量门禁、L2 frontmatter 规范、并行派发说明。

### 步骤

- [ ] **3.1** 在 skill 文件 Phase 1 结束后追加以下内容：

```markdown
---

## Phase 2：深度分析（并行 subagent 派发）

### 目标
为每个命中的概念维度派 subagent，产出 L2 页面。所有 subagent 独立运行，互不依赖。

### 执行步骤

**步骤 1：确定 subagent 列表**
- 从 Phase 1 的 module-map.md 中提取「是否派 subagent = 是」的概念维度
- 每个概念维度派一个 subagent
- 并行度：≤ 12（与概念维度总数一致）

**步骤 2：获取 source_version**
- 运行 `git -C {source-path} log -1 --format="%H %ai"` 获取最新 commit hash 和日期
- 若 git 命令失败（非 git 仓库），使用 `"unknown"` 并记录警告

**步骤 3：并行派发 subagent**
使用下方 subagent prompt 模板，为每个概念维度并行派发 subagent。
使用 superpowers:dispatching-parallel-agents skill 管理并行执行。

**步骤 4：等待所有 subagent 完成**
- 收集成功产出的 L2 文件列表
- 记录失败的 subagent（概念维度名 + 错误原因）
- 更新 meta.yaml 的 `concepts_covered` 和 `l2_files` 字段
- 失败项记入 REVIEW.md（Phase 6 处理），不终止执行

### Subagent Prompt 模板（完整版，每次调用时填入变量）

```
你是一个知识库 L2 页面撰写专家。

## 任务
为概念维度「{concept}」分析源项目「{source}」的相关实现，产出完整 L2 页面。

## 输入信息
- 概念维度：{concept}
- 概念定义：{concept-definition}（来自 ontology.yaml）
- 源项目路径：{source-path}
- 相关模块：{module-list}（逗号分隔，如 src/memory/, src/utils/persistence/）
- 源版本：{source-version}
- L2 模板路径：{kb-path}/schema/page-templates/L2.md
- 输出路径：{kb-path}/drafts/{source}/wiki/_impl/{concept}--{source}.md

## 分析要求
1. **深度阅读源码**：读取上述相关模块的源文件（不是 README 或文档），追踪实际函数调用链
2. **禁止使用伪代码**：关键代码路径章节必须包含真实函数签名和调用链
3. **先读模板**：读取 L2 模板文件，严格按模板结构输出

## 必含章节（四项缺一不可）

| 章节 | 内容要求 |
|------|---------|
| ## 架构分析 | 模块整体设计，主要抽象和数据流向 |
| ## 关键代码路径 | 核心功能的代码级追踪（真实函数签名 + 调用链） |
| ## 设计亮点 | 值得借鉴的创新点或独特选择 |
| ## 局限性 | 已知缺陷、设计权衡代价、使用场景限制 |

## 输出文件 frontmatter 格式

```yaml
---
title: "{concept}——{source}"
category: L2
source: "{source}"
source_version: "{source-version}"
concept: "{concept}"
created: "{YYYY-MM-DD}"
confidence: high | medium | low
---
```

confidence 判断标准：
- high：相关模块代码量充足，分析有足够证据支撑
- medium：部分逻辑不清晰，有推断成分
- low：相关代码极少或高度抽象，分析主要依赖推断

## 完成后输出
写入文件到指定输出路径，然后输出一行确认：
`[L2 DONE] {concept}--{source}.md 已写入，confidence={high/medium/low}`
```

### L2 质量门禁（写完后自检）

每个 L2 文件必须满足以下全部条件，否则记入 Phase 5 阻断问题：

```
[ ] frontmatter 包含所有必填字段（title, category, source, source_version, concept, created, confidence）
[ ] 包含 ## 架构分析 章节（有实质内容，非占位符）
[ ] 包含 ## 关键代码路径 章节（包含真实函数签名，非伪代码）
[ ] 包含 ## 设计亮点 章节（有实质内容）
[ ] 包含 ## 局限性 章节（有实质内容）
[ ] 文件命名符合规范：{concept}--{source}.md
```
```

- [ ] **3.2** git commit：`feat(skill): add Phase 2 parallel subagent dispatch with complete L2 prompt template`

---

## Task 4：Phase 3 — L1 聚合与语义化 patch

> 目标：写入 Phase 3 完整指令，包括如何读取现有 L1 页面、语义化 patch 格式（OP-1/OP-2/OP-3 完整模板）、必须包含的内容要求。

### 步骤

- [ ] **4.1** 在 skill 文件 Phase 2 结束后追加以下内容：

```markdown
---

## Phase 3：L1 聚合（语义化 patch）

### 目标
基于新产出的 L2 页面，为现有 L1 页面生成语义化增量 patch。
**不直接修改 wiki/ 下的任何文件**，所有 patch 写入 drafts/ 隔离目录。

### 执行步骤

**步骤 1：收集 L2 产出**
- 读取所有 Phase 2 产出的 L2 文件（`drafts/{source}/wiki/_impl/*.md`）
- 提取每个 L2 文件的 `concept` 字段，确定对应哪个 L1 页面

**步骤 2：读取现有 L1 页面**
- 对每个涉及的概念维度，尝试读取 `{kb-path}/wiki/{concept}.md`
- 若 L1 不存在：读取 `{kb-path}/schema/page-templates/L1.md` 作为基础，在 patch 中注明「新建 L1」

**步骤 3：差异分析**
分析新源的设计与已有 L1 内容的差异：
- 对比表中是否已有 {source} 列？（没有则需要 OP-1）
- 设计权衡中是否有 {source} 带来的新场景决策？（没有则需要 OP-2）
- 新源是否揭示了概念间的新关系？（有则需要 OP-3）

**步骤 4：产出 patch 文件**
为每个 L1 页面写入 `{kb-path}/drafts/{source}/wiki/{concept}.md.patch`

### Patch 文件格式（完整模板）

```markdown
# L1 语义化 Patch：{concept} ← {source}

**生成时间**: {YYYY-MM-DD HH:MM}
**基于 L2**: drafts/{source}/wiki/_impl/{concept}--{source}.md
**目标 L1**: wiki/{concept}.md（{"已存在" | "新建，基于 L1 模板"}）

---

## 操作列表

### OP-1: 在"各家设计对比"表中新增一列
**位置**：`## 各家设计对比` → 对比表格
**操作**：在现有对比表中新增列 `{source}`
**新增内容**（按现有表格行维度填充）：

| 维度 | {source} |
|------|----------|
| 实现方式 | {具体实现描述，来自 L2 架构分析} |
| 关键特性 | {核心特性，来自 L2 设计亮点} |
| 适用场景 | {适用场景，来自 L2 局限性反推} |
| 主要局限 | {主要局限，来自 L2 局限性} |

> 若表格中不存在某行维度，根据 L2 内容自行补充，保持与现有列一致的维度粒度。

---

### OP-2: 在"设计权衡"中补充场景决策
**位置**：`## 设计权衡` → 场景决策指南
**操作**：追加以下场景决策建议

**当 {具体场景描述} 时，优先考虑 {source} 的 {具体做法}**
- 原因：{基于 L2 分析的依据}
- 证据：见 `wiki/_impl/{concept}--{source}.md § 设计亮点`
- 对比：与 {已有源} 的差异在于 {关键差异点}

> 若无新场景可补充（新源与已有来源设计高度相似），写 `OP-2: 无新场景决策可补充，设计与已有来源高度一致`

---

### OP-3: 在"关系"中新增关联（可选）
**位置**：`## 关系`
**操作**：追加以下关系声明（仅当 L2 揭示了新的跨概念关系时填写）

- {source} 的实现 `{supports | contradicts | evolved_into | depends_on}` [[{related-concept}]]
  - 证据：{来自 L2 的具体代码路径或设计描述}

> 若无新关系，写 `OP-3: 无新跨概念关系发现`

---

## Patch 说明

- **人工 apply 方式**：阅读每条 OP 操作，手动编辑对应 `wiki/{concept}.md`
- **apply 顺序**：建议按 OP-1 → OP-2 → OP-3 顺序执行
- **注意**：OP-1 要求表格格式与现有 L1 保持一致（列数、对齐方式）
```

### 重要约束

- **每个 L1 patch 必须包含 OP-1（对比列）和 OP-2（场景决策）**，即使内容是「无新内容可补充」也要明确写出
- **禁止在 patch 中使用「待填写」「TBD」等占位符**，每个操作必须有具体内容或明确的「无新内容」说明
- patch 文件是给人工 review 用的，措辞要清晰，位置指引要精确到章节名

### 更新 meta.yaml

Phase 3 完成后，更新 `drafts/{source}/meta.yaml`：
```yaml
l1_patches:
  - wiki/{concept-1}.md.patch
  - wiki/{concept-2}.md.patch
  - ...
```
```

- [ ] **4.2** git commit：`feat(skill): add Phase 3 L1 semantic patch instructions with complete patch template`

---

## Task 5：Phase 4 — 覆盖率扫描

> 目标：写入 Phase 4 完整指令，包括目录扫描步骤、概念交叉检查、自动补漏机制（最多一轮）、覆盖率计算公式（含排除规则）、coverage-report.md 完整格式。

### 步骤

- [ ] **5.1** 在 skill 文件 Phase 3 结束后追加以下内容：

```markdown
---

## Phase 4：覆盖率扫描

### 目标
防止注意力遗漏，确保源项目核心模块全部被分析。覆盖率 ≥ 80% 为通过门禁。

### 覆盖率计算规则

```
覆盖率 = 有对应 L2 的顶层目录数 / 有效顶层目录总数

有效顶层目录（计入分母）：实现源码目录，如 src/, core/, lib/, agents/, models/ 等
排除目录（不计入分母）：
  - 配置类：.github/, .circleci/, .vscode/, config/（纯配置）
  - 文档类：docs/, README.md, CHANGELOG.md
  - 测试类：tests/, test/, spec/, __tests__/
  - 示例类：examples/, demo/, sample/
  - 构建类：scripts/, tools/, ci/, bin/, dist/, build/

覆盖认定标准：
  - L2 文件中明确提及并分析了该模块（非泛泛提及）
  - 一个 L2 文件可覆盖多个模块（若分析内容涵盖这些模块）
```

### 执行步骤

**步骤 1：目录扫描（防遗漏）**
- 列出源码所有顶层目录（与 Phase 1 保持一致）
- 逐个判断：是否已被某个 L2 文件覆盖
  - 检查所有 L2 文件内容，搜索该模块路径名是否被明确分析
- 计算初始覆盖率

**步骤 2：概念交叉检查**
- 对每个概念维度，用关键词搜索源码（用 Grep 工具，非 bash grep）
- 关键词来源：ontology.yaml 的 aliases + 下方补充关键词表
- 找出「关键词命中但已有 L2 中未提及」的实现
- 列出「遗漏发现」清单

**附录：概念关键词扩展参考**
| 概念维度 | 补充搜索关键词 |
|---------|-------------|
| memory-system | dream, archive, consolidate, compact, recall, forget, persist |
| query-loop | loop, run, step, turn, cycle, execute, dispatch, agentic |
| tool-system | tool, call, invoke, register, executor, permission, sandbox |
| multi-agent | orchestrat, worker, spawn, delegate, coordinate, handoff, swarm |
| context-management | context, compress, summarize, window, token, truncate, prune |
| hooks | hook, event, lifecycle, pre_tool, post_tool, intercept, middleware |
| mcp-skills | mcp, plugin, extension, server, client, stdio, transport |
| session-recovery | recovery, resume, checkpoint, snapshot, crash, restore |
| channel-remote | channel, websocket, sse, streaming, fastapi, http, interface |
| evaluation-observability | eval, metric, logging, trace, benchmark, observer, span |
| prompt-system | prompt, system_prompt, template, inject, section, priority |
| runtime-state | state, session, storage, persist, serialize, checkpoint |

**步骤 3：自动补漏（最多一轮）**
- 若发现遗漏模块：
  1. 对每个遗漏项，回到 Phase 2 派补漏 subagent（使用相同 prompt 模板）
  2. 等待补漏 subagent 完成
  3. 重新计算覆盖率
- **最多补一轮**：若补漏后仍有遗漏，记录到 coverage-report.md，不再循环

**步骤 4：覆盖率门禁判断**
- 覆盖率 ≥ 80%：PASS，继续 Phase 5
- 覆盖率 < 80%：WARN，记录到 coverage-report.md，并在 Phase 6 的 REVIEW.md 中标注 BLOCKED 问题，**不终止执行**（继续产出后续报告）

### 产出文件：`drafts/{source}/analysis/coverage-report.md`

```markdown
# 覆盖率扫描报告：{source}

**生成时间**: {YYYY-MM-DD HH:MM}

## 汇总

| 指标 | 数值 |
|------|------|
| 有效顶层模块总数 | N |
| 已覆盖模块数 | M |
| 覆盖率 | M/N = XX% |
| 状态 | PASS（≥80%）/ WARN（<80%）|
| 补漏轮次 | 0 / 1 |

## 模块覆盖详情

| 模块路径 | 对应 L2 | 状态 |
|---------|---------|------|
| src/core/ | query-loop--{source}.md | 已覆盖 |
| src/memory/ | memory-system--{source}.md | 已覆盖 |
| src/tools/ | tool-system--{source}.md | 已覆盖 |
| src/utils/ | —— | 未覆盖（工具类，已排除低优先级） |
| src/plugins/ | —— | 未覆盖（补漏 subagent 已派发） |

## 排除模块

| 路径 | 排除原因 |
|------|---------|
| docs/ | 文档目录 |
| tests/ | 测试代码 |
| .github/ | CI/CD 配置 |

## 概念交叉检查发现

| 概念维度 | 关键词 | 发现 | 处理 |
|---------|-------|------|------|
| memory-system | dream/archive | 发现 dream_manager.py 未被 L2 提及 | 已派补漏 subagent |
| tool-system | sandbox | 未发现相关实现 | 无需处理 |

## 补漏执行记录

- 补漏 subagent 1：`memory-system--{source}.md` 补充 dream_manager 分析（{datetime}）
- （若无补漏，写：本轮无需补漏）
```
```

- [ ] **5.2** git commit：`feat(skill): add Phase 4 coverage scan with formula, keyword table, and report template`

---

## Task 6：Phase 5 — 分级 Review 分类

> 目标：写入 Phase 5 完整指令，包括三级分类规则（自动修复/标注待确认/阻断）、每级的判断条件和具体示例、处理方式。

### 步骤

- [ ] **6.1** 在 skill 文件 Phase 4 结束后追加以下内容：

```markdown
---

## Phase 5：分级 Review 分类

### 目标
对所有产出进行质量审查，按严重程度分三级处理。**不暂停执行**，所有问题分类记录到 REVIEW.md（Phase 6 输出）。

### 审查范围

1. 所有 L2 文件（`drafts/{source}/wiki/_impl/*.md`）
2. 所有 L1 patch 文件（`drafts/{source}/wiki/*.md.patch`）
3. 覆盖率报告（`drafts/{source}/analysis/coverage-report.md`）
4. meta.yaml 完整性

### 三级分类规则

---

#### 级别 A：自动修复（小问题）

**判断条件**：可机械判断和修正，无需人工判断
**处理方式**：直接修复，写入 REVIEW.md「自动修复清单」

| 问题类型 | 判断标准 | 修复方式 |
|---------|---------|---------|
| frontmatter 缺字段 | 对比必填字段列表，有缺失 | 补充默认值（如 `confidence: medium`）|
| 文件命名不规范 | 文件名不符合 `{concept}--{source}.md` 格式 | 重命名文件 |
| Markdown 标题层级错误 | 如 `####` 用于二级内容 | 修正为正确层级 |
| L2 必含章节标题缺失（内容已有） | 内容存在但标题写法不标准 | 补充标准标题 |
| meta.yaml 字段类型错误 | 如 `coverage_rate: "85%"`（应为 `0.85`）| 修正类型 |

**示例**：
```
[AUTO-FIX-1] drafts/{source}/wiki/_impl/memory-system--{source}.md
  问题：frontmatter 缺少 source_version 字段
  修复：补充 source_version: "unknown"
```

---

#### 级别 B：标注待确认（中等问题）

**判断条件**：需要人工判断，但不影响整体结构
**处理方式**：写入 REVIEW.md「待确认问题清单」，标注位置和建议

| 问题类型 | 判断标准 | 处理 |
|---------|---------|------|
| L2 内容较浅 | confidence=low 或架构分析少于 300 字 | 标注建议补充 |
| 关联关系不确定 | OP-3 中关系类型选择存疑 | 标注两种可能性 |
| 设计权衡依据不足 | 场景决策建议缺乏代码证据 | 标注「需要补充证据」|
| 概念维度映射模糊 | 某模块被映射到多个概念，可能重复 | 标注「请确认主归属」|
| source_version 为 unknown | git log 失败，版本信息缺失 | 标注「请手动补充版本」|

**示例**：
```
[MEDIUM-1] L2 内容较浅
  位置：drafts/{source}/wiki/_impl/context-management--{source}.md § 架构分析
  问题：架构分析仅 150 字，未追踪完整数据流，confidence=low
  建议：重新阅读 src/context/ 目录，补充 TokenBudget 和压缩触发逻辑
```

---

#### 级别 C：阻断（大问题）

**判断条件**：可能污染知识库或造成错误决策
**处理方式**：写入 REVIEW.md「阻断问题清单」，不暂停执行（继续后续 phase）

| 问题类型 | 判断标准 |
|---------|---------|
| L2 与已有 L1 存在明显矛盾 | 对同一组件的描述截然相反 |
| 重大架构判断分歧 | 对某设计模式的优劣判断与已有结论相反 |
| L2 缺失必含章节 | 四项质量门禁（架构分析/代码路径/设计亮点/局限性）未全部满足 |
| 覆盖率低于 80% | coverage-report.md 状态为 WARN |
| L2 写入失败 | Phase 2 中某 subagent 未能产出文件 |

**示例**：
```
[BLOCK-1] L2 缺失必含章节
  位置：drafts/{source}/wiki/_impl/tool-system--{source}.md
  问题：缺少「## 局限性」章节，仅有架构分析、代码路径、设计亮点
  风险：不符合 L2 质量门禁，合并后会导致知识库标准不一致
  建议：补写局限性章节后再 apply

[BLOCK-2] 覆盖率未达门禁
  位置：drafts/{source}/analysis/coverage-report.md
  问题：覆盖率 72%（低于 80% 门禁），有 3 个有效模块未被分析
  风险：ingest 不完整，wiki 更新后可能产生知识盲区
  建议：手动补充遗漏模块的 L2 分析后再 apply
```

### 统计分级结果

Phase 5 完成后，统计：
- `auto_fixed`：自动修复数量
- `pending_review`：待确认数量
- `blocked`：阻断数量

更新 `meta.yaml` 的 `review_summary` 字段。
```

- [ ] **6.2** git commit：`feat(skill): add Phase 5 tiered review classification with concrete examples for all three levels`

---

## Task 7：Phase 6 — 报告生成

> 目标：写入 Phase 6 完整指令，包括 REVIEW.md 完整模板、meta.yaml 最终状态模板、状态判定逻辑（READY_TO_MERGE/NEEDS_REVIEW/BLOCKED）。

### 步骤

- [ ] **7.1** 在 skill 文件 Phase 5 结束后追加以下内容：

```markdown
---

## Phase 6：产出报告

### 目标
生成最终 review 报告（`REVIEW.md`），更新 `meta.yaml` 为最终状态。

### 状态判定逻辑

| 状态 | 判定条件 |
|------|---------|
| `READY_TO_MERGE` | `blocked == 0` 且 `pending_review == 0` 且 `coverage_rate >= 0.80` |
| `NEEDS_REVIEW` | `blocked == 0` 且（`pending_review > 0` 或任何 confidence=low 的 L2）|
| `BLOCKED` | `blocked > 0` 或 `coverage_rate < 0.80` |

> 优先级：BLOCKED > NEEDS_REVIEW > READY_TO_MERGE（有任一阻断问题即为 BLOCKED）

### 产出文件一：`drafts/{source}/REVIEW.md`（完整模板）

```markdown
# Ingest Review 报告：{source}

**Ingest 时间**: {YYYY-MM-DD HH:MM:SS}
**源路径**: {source-path}
**源版本**: {source-version}
**知识库路径**: {kb-path}
**状态**: READY_TO_MERGE | NEEDS_REVIEW | BLOCKED

---

## 概览

| 指标 | 数值 |
|------|------|
| 新增 L2 页面 | N 个 |
| 更新 L1 页面（patch 数） | M 个 |
| 源码覆盖率 | XX%（PASS / WARN）|
| 自动修复问题 | K 个 |
| 待确认问题 | P 个 |
| 阻断问题 | Q 个 |

### 产出文件清单

**L2 页面（新增）**：
- `drafts/{source}/wiki/_impl/{concept-1}--{source}.md`（confidence=high）
- `drafts/{source}/wiki/_impl/{concept-2}--{source}.md`（confidence=medium）
- ...

**L1 Patch（待 apply）**：
- `drafts/{source}/wiki/{concept-1}.md.patch`
- `drafts/{source}/wiki/{concept-2}.md.patch`
- ...

**分析过程文件**：
- `drafts/{source}/analysis/module-map.md`
- `drafts/{source}/analysis/coverage-report.md`

---

## 自动修复清单（已处理，无需人工干预）

- [x] {文件路径}：{问题描述} → {修复方式}
- [x] ...
（若无自动修复项，写：本次 ingest 无需自动修复）

---

## 待确认问题清单

（若无待确认问题，写：无待确认问题）

### [MEDIUM-1] {问题标题}
- **位置**: `{文件路径}` § {章节名}
- **问题**: {具体问题描述}
- **建议**: {处理建议}

### [MEDIUM-2] {问题标题}
- **位置**: `{文件路径}` § {章节名}
- **问题**: {具体问题描述}
- **建议**: {处理建议}

---

## 阻断问题清单

> ⚠️ 以下问题建议在 apply 到 wiki/ 前人工确认和修复

（若无阻断问题，写：无阻断问题，可安全 apply）

### [BLOCK-1] {问题标题}
- **位置**: `{文件路径}` § {章节名}
- **问题**: {具体问题描述}
- **风险**: {若直接合并可能造成的影响}
- **建议修复**: {具体修复步骤}

---

## 合并操作指南

确认上述问题后，按以下顺序执行合并：

1. **L2 合并**：将 `drafts/{source}/wiki/_impl/*.md` 复制到 `{kb-path}/wiki/_impl/`
2. **L1 patch apply**：逐个阅读 `drafts/{source}/wiki/*.md.patch`，按 OP 操作说明手动编辑对应 `wiki/{concept}.md`
3. **验证**：检查 wiki/ 内部链接是否完整，新 L2 是否被 L1 正确引用
4. **清理**：确认无误后删除 `drafts/{source-name}/`
5. **git commit**：`feat(wiki): ingest {source} — N L2 pages, M L1 patches`
```

### 产出文件二：`drafts/{source}/meta.yaml`（最终状态模板）

```yaml
source_name: "{source-name}"
source_path: "{original-path}"
source_version: "{git-hash-or-version}"
ingest_time: "{YYYY-MM-DDThh:mm:ssZ}"
kb_path: "{knowledge-base-root}"
concepts_covered:
  - agent-loop
  - memory-system
  - tool-system
  - ...
coverage_rate: 0.85      # 小数，非百分比字符串
l2_files:
  - wiki/_impl/agent-loop--{source}.md
  - wiki/_impl/memory-system--{source}.md
  - ...
l1_patches:
  - wiki/agent-loop.md.patch
  - wiki/memory-system.md.patch
  - ...
status: READY_TO_MERGE   # READY_TO_MERGE | NEEDS_REVIEW | BLOCKED
review_summary:
  auto_fixed: 0          # 整数
  pending_review: 0      # 整数
  blocked: 0             # 整数
notes: ""                # 可选：人工备注
```

> **注意**：`coverage_rate` 写小数（如 `0.85`），非 `"85%"` 字符串
```

- [ ] **7.2** git commit：`feat(skill): add Phase 6 report generation with complete REVIEW.md and meta.yaml templates`

---

## Task 8：Phase 7 — 自优化反馈 + Schema 骨架文件

> 目标：写入 Phase 7 完整指令（经验追加、策略蒸馏触发、agent 启动加载顺序），创建 schema/ingest-strategies.yaml 和 schema/ingest-learnings.md 骨架文件。

### 步骤

- [ ] **8.1** 在 skill 文件 Phase 6 结束后追加以下内容：

```markdown
---

## Phase 7：自优化反馈

### 目标
Agent 从每次 ingest 中积累经验，逐步优化分析策略。
核心原则：经验追加自动化，策略更新走人工把关流程。

### 7.1 每次 ingest 完成后（自动执行）

回顾本次 ingest 表现，追加经验条目到 `{kb-path}/schema/ingest-learnings.md`：

**追加格式**（追加到文件末尾，不修改已有内容）：

```markdown

### {source} — {YYYY-MM-DD}
- 覆盖率: {XX}%，补漏 {N} 轮（{0|1}）
- 发现：{具体观察，如「该项目把 memory 实现藏在 utils/persistence/ 下，与模块名不对应」}
- 教训：{可泛化的经验，如「Python 项目的 utils/ 子目录不能跳过，需深度扫描」}
- L2 质量分布：high={N} medium={N} low={N}
- 阻断问题：{数量及简述，如「1个——tool-system L2 缺失局限性章节」}
- 待确认问题：{数量及简述}
```

### 7.2 策略蒸馏（每 5 次 ingest 后，或手动触发）

触发条件：`schema/ingest-learnings.md` 中的条目数达到 5 的倍数时，或用户明确请求。

**蒸馏流程**：
1. 读取 `schema/ingest-learnings.md` 所有条目
2. 读取 `schema/ingest-strategies.yaml` 现有策略（避免重复）
3. 提炼可执行策略规则（跨多次 ingest 反复出现的模式）
4. **策略走 draft → review 流程**：
   - 写入 `{kb-path}/drafts/strategy-update/ingest-strategies.yaml.patch`
   - patch 格式：YAML，包含 `add`（新增策略）和 `deprecate`（废弃策略）两个列表
5. 在 REVIEW.md 结尾追加提示：`[Phase 7] 策略蒸馏草稿已写入 drafts/strategy-update/`
6. 人工确认后，将 patch apply 到 `schema/ingest-strategies.yaml`

**策略蒸馏 patch 格式**（`drafts/strategy-update/ingest-strategies.yaml.patch`）：

```yaml
# 策略蒸馏建议 — {YYYY-MM-DD}
# 基于经验：{source-1}, {source-2}, ...

add:
  - id: python-utils-deep-scan
    trigger: "源项目主语言 == Python"
    rule: "utils/ 和 lib/ 子目录必须深度扫描（逐文件列出），不列入覆盖率排除名单"
    evidence: "mempalace ingest 发现 dream_manager.py 在 utils/persistence/；hermes ingest 发现 hook 实现在 lib/events/"
    added: "{YYYY-MM-DD}"

  - id: {策略-id}
    trigger: "{触发条件}"
    rule: "{可执行规则描述}"
    evidence: "{来自哪些 ingest 的证据}"
    added: "{YYYY-MM-DD}"

deprecate: []    # 建议废弃的策略 ID 列表（若无则为空数组）
```

### 7.3 Agent 启动加载顺序（Phase 1 开始前）

每次运行 kb-ingest 时，按以下顺序加载配置：

1. 读取 `{kb-path}/schema/ingest-strategies.yaml`（若存在）
   → 解析 `strategies` 列表，作为 Phase 1-6 执行策略的额外指导
   → 打印已加载策略数：`[kb-ingest] 加载执行策略 N 条`

2. 读取 `{kb-path}/schema/ingest-learnings.md`（若存在）
   → 作为参考经验，不直接影响执行逻辑
   → 打印：`[kb-ingest] 已读取 N 条经验记录`

3. 两个文件均不存在时：输出 `[kb-ingest] 首次运行，策略文件将在本次完成后创建`，继续执行

### 合并流程（Review 通过后）

合并步骤由人工执行：
1. 检查 `drafts/strategy-update/ingest-strategies.yaml.patch`
2. 确认每条策略合理、可执行
3. 手动 apply 到 `schema/ingest-strategies.yaml`
4. 清理 `drafts/strategy-update/`
```

- [ ] **8.2** 创建 `{kb-path}/schema/ingest-strategies.yaml` 骨架文件：

**文件路径**：`/Users/neo/Desktop/project/git/ai- knowledge/schema/ingest-strategies.yaml`

**文件内容**：

```yaml
# kb-ingest 执行策略文件
# 由 Phase 7 自动蒸馏生成，人工确认后 apply
# 每次 ingest 启动时自动读取
#
# 格式：
#   strategies:
#     - id:      唯一 ID（kebab-case）
#       trigger: 触发条件（自然语言）
#       rule:    可执行策略规则
#       source:  来源 ingest 项目名（逗号分隔）
#       added:   添加日期（YYYY-MM-DD）

version: "1.0"
strategies: []
# 首条策略示例（请在积累 5 次 ingest 经验后通过蒸馏流程添加真实策略）:
#   - id: python-utils-deep-scan
#     trigger: "源项目主语言 == Python"
#     rule: "utils/ 和 lib/ 子目录必须深度扫描（逐文件列出），不列入覆盖率排除名单"
#     source: "示例：mempalace, hermes"
#     added: "2026-04-15"
```

- [ ] **8.3** 创建 `{kb-path}/schema/ingest-learnings.md` 骨架文件：

**文件路径**：`/Users/neo/Desktop/project/git/ai- knowledge/schema/ingest-learnings.md`

**文件内容**：

```markdown
# Ingest 经验积累

> 每次 kb-ingest 完成后，Phase 7 自动追加经验条目到此文件。
> 每积累 5 条（或手动触发），启动策略蒸馏流程，提炼为 ingest-strategies.yaml。
> 蒸馏完成后，对应条目可归档（移至文件末尾的「已蒸馏」区块）。

## 活跃经验条目

（首次使用，暂无条目。运行第一次 kb-ingest 后自动生成。）

---

## 已蒸馏归档

（经策略蒸馏后的经验条目移至此处，仅作历史记录。）
```

- [ ] **8.4** git commit：`feat(skill): add Phase 7 self-optimization with strategy distillation flow and schema skeleton files`

---

## Task 9：安装 + 验证

> 目标：将 skill 注册到 Claude Code settings，并通过干运行验证 skill 文件格式正确、所有 Phase 都存在、模板无占位符。

### 步骤

- [ ] **9.1** 检查 Claude Code settings，确认 kb-ingest skill 注册路径：
  - 打开 `~/.claude/settings.json`（或 `~/.claude/settings.local.json`）
  - 确认 `skills` 配置块中包含 kb-ingest，或按需添加

- [ ] **9.2** 验证 skill 文件完整性检查清单：

```
[ ] 文件存在于 ~/.claude/skills/kb-ingest/kb-ingest.md
[ ] frontmatter 包含 name 和 description 字段
[ ] 触发条件（Trigger Conditions）章节存在
[ ] 输入规范章节存在（最小输入 + 可选参数）
[ ] 前置依赖验证章节存在（5 项检查）
[ ] 错误处理规范章节存在（6 种错误类型）
[ ] 产出目录结构章节存在
[ ] Phase 1：预分析（含 9 个步骤 + 2 个产出文件模板）
[ ] Phase 2：深度分析（含 subagent prompt 模板完整版 + L2 质量门禁）
[ ] Phase 3：L1 聚合（含 patch 文件完整模板 + OP-1/OP-2/OP-3）
[ ] Phase 4：覆盖率扫描（含计算规则 + coverage-report.md 模板）
[ ] Phase 5：分级 review（含三级规则 + 每级示例）
[ ] Phase 6：产出报告（含 REVIEW.md 模板 + meta.yaml 最终状态模板）
[ ] Phase 7：自优化反馈（含经验追加格式 + 蒸馏 patch 格式 + 启动加载顺序）
[ ] 无「待填写」「TBD」「TODO」等占位符
```

- [ ] **9.3** 验证 schema 骨架文件：

```
[ ] schema/ingest-strategies.yaml 存在，YAML 格式有效，strategies: [] 为空数组
[ ] schema/ingest-learnings.md 存在，包含「活跃经验条目」和「已蒸馏归档」两个章节
```

- [ ] **9.4** 干运行验证（以知识库本身作为测试源）：
  - 用已知小型源项目（如 `raw/claude-code` 中某子目录）作为测试源
  - 调用 skill，确认输出：
    - `[kb-ingest] 加载执行策略` 消息（或首次运行提示）
    - 前置依赖验证通过消息
    - Phase 1 产出 `drafts/{source}/analysis/module-map.md` 和 `drafts/{source}/meta.yaml`
    - Phase 2 subagent 被正确派发（至少派出 1 个）
    - Phase 6 产出 `drafts/{source}/REVIEW.md`
    - Phase 7 在 `schema/ingest-learnings.md` 中追加了经验条目

- [ ] **9.5** git commit：`feat(skill): install and validate kb-ingest skill with complete phase coverage`

---

## 自检清单（计划完成后执行）

以下检查对应 spec 的各个章节：

| Spec 章节 | 覆盖 Task | 已覆盖？ |
|----------|----------|---------|
| §1 项目定位（远程 agent 兼容）| T1（无交互输入设计）| |
| §2 设计原则（7 条）| T1（输出隔离）+ T4（覆盖率门禁）+ T3（L2 确定性）| |
| §3 前置依赖（4 个路径）| T1（Pre-flight Checklist）| |
| §4 输入规范（最小输入 + 3 可选参数）| T1（输入规范章节）| |
| §5 Phase 1（9 个步骤 + 2 产出）| T2 | |
| §5 Phase 2（subagent prompt + L2 门禁 + frontmatter）| T3 | |
| §5 Phase 3（patch 格式 OP-1/OP-2/OP-3）| T4 | |
| §5 Phase 4（覆盖率公式 + 报告格式）| T5 | |
| §5 Phase 5（三级分类 + 示例）| T6 | |
| §5 Phase 6（REVIEW.md + meta.yaml + 状态判定）| T7 | |
| §6 产出目录结构 | T1（产出目录结构章节）| |
| §7 合并流程（人工步骤）| T7（REVIEW.md 合并操作指南）| |
| §8 错误处理（6 种类型）| T1（错误处理规范章节）| |
| §9 Skill 内联规范（自包含 + subagent prompt + 覆盖率规则）| T3 + T5 | |
| §10 与 ai-knowledge 协作关系 | T1（职责定位说明）| |
| §11 Phase 7（经验追加 + 策略蒸馏 + 加载顺序）| T8 | |
| §11 Schema 骨架文件（strategies.yaml + learnings.md）| T8 | |
| §12 实现优先级 | P0 = T2+T3+T7，P1 = T4+T4，P2 = T5+T6+T7+T8 | |

---

*计划版本：2026-04-15 v1.0 — 基于 spec 2026-04-15-kb-ingest-design.md 全量覆盖*
